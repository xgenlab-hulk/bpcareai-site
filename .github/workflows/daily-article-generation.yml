name: Daily Article Generation & Deployment

on:
  # 每天北京时间 10:00 执行 (UTC 02:00)
  schedule:
    - cron: '0 2 * * *'

  # 支持手动触发
  workflow_dispatch:
    inputs:
      articles_count:
        description: 'Override article count (leave empty for auto-detection based on day)'
        required: false
        type: number

# Permissions required for the workflow
permissions:
  contents: write  # Allow pushing commits to the repository

jobs:
  generate-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Configure Git
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

      - name: Run daily article generation
        env:
          QWEN_API_KEY: ${{ secrets.QWEN_API_KEY }}
          ARTICLES_COUNT_OVERRIDE: ${{ inputs.articles_count }}
        run: |
          if [ -n "$ARTICLES_COUNT_OVERRIDE" ]; then
            echo "Starting daily article generation with override count: $ARTICLES_COUNT_OVERRIDE..."
          else
            echo "Starting daily article generation with auto-detection..."
          fi
          npm run auto:generate

      - name: Rebuild articles index
        run: npm run build:articles-index

      - name: Commit and push changes
        run: |
          # Add all generated files
          git add -A

          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            # Get current date for commit message
            TODAY=$(date +'%Y-%m-%d')

            # Count new articles (changed .md files in content/articles/)
            NEW_ARTICLES=$(git diff --staged --name-only | grep -c "content/articles/.*\.md" || true)

            # Create commit message
            COMMIT_MSG="chore: daily generation - ${TODAY} - ${NEW_ARTICLES} articles [skip ci]"

            git commit -m "$COMMIT_MSG"
            git push origin main

            echo "✅ Committed and pushed ${NEW_ARTICLES} new articles"
          fi

      - name: Generate daily report
        if: always()
        run: |
          echo "## Daily Generation Report - $(date +'%Y-%m-%d')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Status**: Generation completed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Count total articles
          TOTAL_ARTICLES=$(find content/articles -name "*.md" | wc -l)
          echo "📊 **Total articles**: ${TOTAL_ARTICLES}" >> $GITHUB_STEP_SUMMARY

          # Check topic inventory
          if [ -f "data/planned-topics-blood-pressure.json" ]; then
            BP_COUNT=$(cat data/planned-topics-blood-pressure.json | grep -c "title" || echo "0")
            echo "📚 **Blood pressure topics remaining**: ${BP_COUNT}" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🚀 **Deployment**: Pushed to GitHub, Vercel auto-deployment triggered" >> $GITHUB_STEP_SUMMARY

      - name: Notify on failure
        if: failure()
        run: |
          echo "## ❌ Daily Generation Failed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Please check the logs for details." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Date**: $(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
